setopt nolistbeep
set nobeep
set matchbeep=never
autoload colors
colors
PROMPT="%{${fg[yellow]}%}%d%{${reset_color}%} 
[%n]$ "

PROMPT2='[%n]> '
#export PATH='/usr/local/bin:$PATH'
export PATH="/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/Users/nf2/Downloads/adt-bundle-mac-x86_64-20130522/sdk/platform-tools:/Users/nf2/bin:/usr/local/opt/coreutils/libexec/gnubin:/Users/nf2/Documents/Programming/SourceCode/ShellScript/sh"
# export PATH='/usr/local/bin:$PATH'
alias relogin='exec $SHELL -l'
alias pc='percol --auto-fail --auto-match'
alias l='ls -a -l'
alias o='less'
alias h='history 0'
alias c='clear'
alias p='pwd'
alias get_file_path_in_this_dir='find `pwd` -maxdepth 1'
alias INC='perl -e `foreach(@INC){print "$_\n";}`'
alias less='less -N'
alias vi ='/usr/bin/vim'
alias lo='ls -a -l | less -N'
alias pd='percol $HOME/dir.info | xargs open'
alias pco='percol $HOME/file.info | xargs open'
alias pcl='percol $HOME/file.info | xargs less'
alias pch='history 0 | percol'
alias hpc='history 0 | percol'
#alias listpm='find `perl -e 'print "@INC"'` -name "*.pm"' 
alias listpm='perl -e `print "@INC"`'
alias s='sudo'
alias ..='cd ../'
alias ...='cd ../../'
alias ....='cd ../../../'
# alias refresh='source .bash_profile'
alias refresh='source ~/.zshrc'
alias srm='srm -s'
alias e='exit'
alias mpeg='open -a MPEC Streamclip'
alias plc='perl -c'
alias pl='perl'
# alias perldoc='perldoc5.12'
alias pld='perldoc5.12'
alias f="open ."
alias lsd="find ./ type -d"
alias lsa="ls -a"
alias g="grep"
alias eject_CD="drutil eject"
alias tidy_space='perl -pi -e "s/\s{2,}+/ /g"'
alias ping_test='ping www.apple.com'
alias noti='terminal-notifier -message "End"'
alias jump_to_site='/Users/nf2/Documents/Programming/SourceCode/Perl/jump_to_site.pl'
alias py2='/Users/nf2/.pyenv/shims/python2.7'
alias py3='/Users/nf2/.pyenv/shims/python3.5'
alias pyf='/Users/nf2/.pyenv/shims/pyflakes'
alias ctags="`brew --prefix` /bin/ctags"

if [ "$(uname)" = 'Darwin' ]; then
	export LSCOLORS=gxfxcxdxbxegedabagacad
	alias ls='ls -G'
fi
# setopt nolistbeep
# setopt list_packed
#
#zsh completion setting
# fpath=(/usr/local/share/zsh-completions(N-/) $fpath)
autoload -Uz compinit
compinit 
zstyle ':completion:*' matcher-list 'm:{a-z}={A-Z}' 
zstyle ':completion::complete:*' use-cache true
zstyle ':completion:*' list-colors "${LS_COLORS}"


function google_en {
   open 'https://www.google.com/webhp?gl=us&hl=en&gws_rd=cr&pws=0' 
}
[[ -s `brew --prefix`/etc/autojump.sh ]] && . `brew --prefix`/etc/autojump.sh
#ベルをならさない
setopt no_beep 
#補完候補をつめて表示
setopt LIST_PACKED
#History
HISTFILE=$HOME/.zhistory
HISTSIZE=10000
SAVEHIST=10000
set History=10000

setopt hist_ignore_dups
setopt extended_history
setopt hist_verify
setopt hist_reduce_blanks
autoload history-search-end

# led cdr available 
# autoload -Uz chpwd_recent_dirs cdr add-zsh-hook
# add-zsh-hook chpwd_recent_dirs

#cdr config
# zstyle ':completion:*' recent-dirs-insert both
# zstyle ':chpwd:*' recent-dirs-max 500
# zstyle ':chpwd:*' recent-dirs-default true
# zstyle ':chpwd:*' recent-dirs-file "$HOME/.cache/shell/chpwd-recent-dirs"
# zstyle ':chpwd:*' recent-dirs-pushed true

#git command PATH
export PATH=/usr/local/git/bin:$PATH
export HOMEBREW_NO_ANALYTICS=1


#Configure for  Python 
# PYTHONPATH=/usr/local/lib/python2.7/site-packages
# export PYTHONPATH

#added PATH to activate pyenv
export PATH=$HOME/.pyenv/bin:$PATH
export PATH=$HOME/.pyenv/shims:$PATH
eval "$(pyenv init -)"
eval "$(pyenv virtualenv-init -)"

#added PATH to activate rbenv
export PATH=$HOME/.rbenv/bin:$PATH
export PATH=$HOME/.rbenv/shims:$PATH
eval "$(rbenv init -)"

#added PATH to activate plenv
export PATH=$HOME/.plenv/shims:$PATH
eval "$(plenv init -)"
